# Optional. If set, the column-mapping engine will call OpenAI (gpt-4o-mini)
# to refine any header it isn't confident about, on top of the built-in
# heuristic mapper. The app works fully without this key.
OPENAI_API_KEY=
